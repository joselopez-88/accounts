server:
  port: 8080
spring:
  application:
    name: accounts
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
# Importando los yml de perfiles creados
  config:
    import:
    - "application_qa.yml"
    - "application_prod.yml"
##
  profiles:
    active:
    - "qa"  
build:
  version: "3.0"    
accounts:
  message: "Welcome to EazyBank accounts related local APIs"
  contactDetails:
    name: "Jose Alberto - Developer"
    email: "alberto.esteva88@gmail.com"
  onCallSupport:
    - "(971)123-4567"
    - "(971)123-5678"